{
  "name": "aic-portal-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "lint:python": "cd backend && ./venv/bin/flake8 app/",
    "format:python": "cd backend && ./venv/bin/black app/ && ./venv/bin/isort app/",
    "build:frontend": "cd frontend && npm run build",
    "test": "cd frontend && npm run build"
  },
  "devDependencies": {
    "husky": "^9.1.6"
  }
}
